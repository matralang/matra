/** Render a normalized Matra tree without coupling Core to an output domain. */
export function renderWith(renderer, ast, options) {
    return renderer.render(ast, options);
}
//# sourceMappingURL=render.js.map